To run, make a dir in your root, called router, path /router , and inside, put the contents in the repository. logo,png, build,sh, etc. next, make a dir called build. so like this /router/build.
next, run it, and if you have aniso, but it inside build as base.iso to skip downloading it. 
yeah thats it. Good Luck
